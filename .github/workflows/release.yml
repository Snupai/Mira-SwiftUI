name: Build and Release

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  check-release:
    runs-on: ubuntu-latest
    outputs:
      should_release: ${{ steps.check.outputs.should_release }}
      version: ${{ steps.check.outputs.version }}
    steps:
      - name: Check commit message for release trigger
        id: check
        env:
          COMMIT_MSG: ${{ github.event.head_commit.message }}
        run: |
          if [[ "$COMMIT_MSG" =~ \[release\ ([0-9]+\.[0-9]+\.[0-9]+)\] ]]; then
            echo "should_release=true" >> $GITHUB_OUTPUT
            echo "version=${BASH_REMATCH[1]}" >> $GITHUB_OUTPUT
            echo "🚀 Release triggered for version ${BASH_REMATCH[1]}"
          else
            echo "should_release=false" >> $GITHUB_OUTPUT
            echo "version=" >> $GITHUB_OUTPUT
            echo "ℹ️ No release trigger found in commit message"
          fi

  build:
    needs: check-release
    if: needs.check-release.outputs.should_release == 'true'
    runs-on: macos-14
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Set Xcode Developer Directory
        run: |
          echo "DEVELOPER_DIR=$(xcode-select -p)" >> $GITHUB_ENV
          echo "Using Xcode at: $(xcode-select -p)"

      - name: Update version in scripts
        run: |
          VERSION="${{ needs.check-release.outputs.version }}"
          sed -i '' "s/VERSION=\"[0-9.]*\"/VERSION=\"$VERSION\"/" bundle.sh
          sed -i '' "s/VERSION=\"[0-9.]*\"/VERSION=\"$VERSION\"/" create-dmg.sh

      - name: Build Release
        run: swift build -c release

      - name: Create App Bundle
        run: ./bundle.sh

      - name: Import Code Signing Certificate
        env:
          CERTIFICATE_BASE64: ${{ secrets.CERTIFICATE_BASE64 }}
          CERTIFICATE_PASSWORD: ${{ secrets.CERTIFICATE_PASSWORD }}
        run: |
          # Create a temporary keychain
          KEYCHAIN_PATH=$RUNNER_TEMP/signing.keychain-db
          KEYCHAIN_PASSWORD="temp_keychain_pwd_123"

          # Create keychain
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          # Decode and import certificate
          echo "$CERTIFICATE_BASE64" | base64 --decode > $RUNNER_TEMP/certificate.p12

          # Import with all applications allowed
          security import $RUNNER_TEMP/certificate.p12 \
            -P "$CERTIFICATE_PASSWORD" \
            -A \
            -t cert \
            -f pkcs12 \
            -k "$KEYCHAIN_PATH" \
            -T /usr/bin/codesign \
            -T /usr/bin/security

          # Set keychain as default and add to search list
          security list-keychain -d user -s "$KEYCHAIN_PATH" $(security list-keychain -d user | tr -d '"')
          security default-keychain -s "$KEYCHAIN_PATH"

          # Allow codesign to access without prompt
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"

          # Debug: List what's in the keychain
          echo "📋 Certificates in keychain:"
          security find-identity -v -p codesigning "$KEYCHAIN_PATH" || echo "No codesigning identities found"
          security find-identity -v "$KEYCHAIN_PATH" || echo "No identities found at all"

          # Store for later steps
          echo "KEYCHAIN_PATH=$KEYCHAIN_PATH" >> $GITHUB_ENV
          echo "KEYCHAIN_PASSWORD=$KEYCHAIN_PASSWORD" >> $GITHUB_ENV

      - name: Sign App
        env:
          PROVISION_PROFILE_BASE64: ${{ secrets.PROVISION_PROFILE_BASE64 }}
        run: |
          # List available identities for debugging
          echo "📋 Available signing identities:"
          security find-identity -v -p codesigning "$KEYCHAIN_PATH"

          # Try to find any valid identity
          IDENTITY=$(security find-identity -v -p codesigning "$KEYCHAIN_PATH" | grep -E "^\s+\d+\)" | head -1 | sed 's/.*"\(.*\)".*/\1/')

          if [ -z "$IDENTITY" ]; then
            echo "⚠️ No codesigning identity found, trying all identities..."
            IDENTITY=$(security find-identity -v "$KEYCHAIN_PATH" | grep -E "^\s+\d+\)" | head -1 | sed 's/.*"\(.*\)".*/\1/')
          fi

          SIGN_IDENTITY="${IDENTITY:--}"
          echo "🔐 Using identity: $SIGN_IDENTITY"

          # Embed provisioning profile if provided
          if [ -n "$PROVISION_PROFILE_BASE64" ]; then
            echo "📦 Embedding provisioning profile"
            echo "$PROVISION_PROFILE_BASE64" | base64 --decode > Mira.app/Contents/embedded.provisionprofile
          fi

          # === PROPER SIGNING (inside-out, no --deep, with --timestamp) ===
          # Following Apple's guidelines: https://developer.apple.com/documentation/security/customizing-the-notarization-workflow
          
          SIGN_FLAGS="--force --timestamp --options runtime"
          ENTITLEMENTS_ARG=""
          if [ -f "Mira.entitlements" ]; then
            ENTITLEMENTS_ARG="--entitlements Mira.entitlements"
          fi

          if [ "$SIGN_IDENTITY" = "-" ]; then
            echo "⚠️ Ad-hoc signing - notarization will NOT work"
            codesign --force --deep --sign - $ENTITLEMENTS_ARG Mira.app
          else
            echo "🔐 Signing with Developer ID (inside-out, proper method)..."
            
            # 1. Sign Sparkle framework components (inside-out)
            if [ -d "Mira.app/Contents/Frameworks/Sparkle.framework" ]; then
              echo "   Signing Sparkle.framework components..."
              
              # Find ALL Mach-O binaries and sign them (inside-out order)
              # Sign XPC services first (deepest nested bundles)
              find Mira.app/Contents/Frameworks/Sparkle.framework -name "*.xpc" -type d | while read xpc; do
                echo "   → Signing XPC: $(basename "$xpc")"
                codesign $SIGN_FLAGS --sign "$SIGN_IDENTITY" "$xpc"
              done
              
              # Sign helper apps
              find Mira.app/Contents/Frameworks/Sparkle.framework -name "*.app" -type d | while read app; do
                echo "   → Signing helper: $(basename "$app")"
                codesign $SIGN_FLAGS --sign "$SIGN_IDENTITY" "$app"
              done
              
              # Sign ALL executables (Autoupdate, Updater, etc.) - these are Mach-O binaries without extensions
              find Mira.app/Contents/Frameworks/Sparkle.framework/Versions -type f -perm +111 ! -name "*.dylib" ! -name ".*" | while read binary; do
                # Check if it's a Mach-O binary
                if file "$binary" | grep -q "Mach-O"; then
                  echo "   → Signing executable: $(basename "$binary")"
                  codesign $SIGN_FLAGS --sign "$SIGN_IDENTITY" "$binary"
                fi
              done
              
              # Sign dylibs
              find Mira.app/Contents/Frameworks/Sparkle.framework -name "*.dylib" -type f | while read dylib; do
                echo "   → Signing dylib: $(basename "$dylib")"
                codesign $SIGN_FLAGS --sign "$SIGN_IDENTITY" "$dylib"
              done
              
              # Sign the framework bundle itself
              echo "   → Signing Sparkle.framework"
              codesign $SIGN_FLAGS --sign "$SIGN_IDENTITY" "Mira.app/Contents/Frameworks/Sparkle.framework"
            fi
            
            # 2. Sign any other frameworks
            find Mira.app/Contents/Frameworks -maxdepth 1 -name "*.framework" -type d ! -name "Sparkle.framework" | while read fw; do
              echo "   → Signing framework: $(basename "$fw")"
              codesign $SIGN_FLAGS --sign "$SIGN_IDENTITY" "$fw"
            done
            
            # 3. Sign the main executable
            echo "   → Signing main executable"
            codesign $SIGN_FLAGS --sign "$SIGN_IDENTITY" $ENTITLEMENTS_ARG "Mira.app/Contents/MacOS/Mira"
            
            # 4. Sign the entire app bundle
            echo "   → Signing app bundle"
            codesign $SIGN_FLAGS --sign "$SIGN_IDENTITY" $ENTITLEMENTS_ARG Mira.app
          fi
          
          echo "📋 Verifying signature..."
          codesign --verify --verbose=2 Mira.app
          
          echo "📋 Checking entitlements..."
          codesign -d --entitlements - Mira.app 2>/dev/null | head -20 || true
          
          echo "✅ App signed properly (inside-out, with timestamp)"

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Create DMG
        run: |
          VERSION="${{ needs.check-release.outputs.version }}"
          ./create-dmg.sh
          mv "Mira-$VERSION.dmg" "Mira-$VERSION-macOS.dmg"

      - name: Create PKG
        run: ./create-pkg.sh

      - name: Notarize App
        timeout-minutes: 30
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          VERSION="${{ needs.check-release.outputs.version }}"
          DMG_FILE="Mira-${VERSION}-macOS.dmg"
          PKG_FILE="Mira-Installer.pkg"

          # Skip notarization if credentials not set
          if [ -z "$APPLE_ID" ] || [ -z "$APPLE_APP_PASSWORD" ] || [ -z "$APPLE_TEAM_ID" ]; then
            echo "⚠️ Notarization credentials not set - skipping notarization"
            echo "   Set APPLE_ID, APPLE_APP_PASSWORD, and APPLE_TEAM_ID secrets to enable"
            exit 0
          fi

          notarize_file() {
            local FILE="$1"
            local NAME="$2"
            
            echo "📤 Submitting $NAME for notarization..."
            
            # Submit and capture output
            if ! xcrun notarytool submit "$FILE" \
              --apple-id "$APPLE_ID" \
              --password "$APPLE_APP_PASSWORD" \
              --team-id "$APPLE_TEAM_ID" \
              --wait 2>&1 | tee notarize-output.txt; then
              
              echo "❌ Notarization command failed for $NAME"
              
              # Try to extract submission ID and get log
              SUBMISSION_ID=$(grep -o 'id: [a-f0-9-]*' notarize-output.txt | head -1 | cut -d' ' -f2)
              if [ -n "$SUBMISSION_ID" ]; then
                echo "📋 Fetching detailed log for $SUBMISSION_ID..."
                xcrun notarytool log "$SUBMISSION_ID" \
                  --apple-id "$APPLE_ID" \
                  --password "$APPLE_APP_PASSWORD" \
                  --team-id "$APPLE_TEAM_ID" || true
              fi
              return 1
            fi
            
            # Check if status is Invalid
            if grep -q "status: Invalid" notarize-output.txt; then
              echo "❌ Notarization rejected for $NAME"
              SUBMISSION_ID=$(grep -o 'id: [a-f0-9-]*' notarize-output.txt | head -1 | cut -d' ' -f2)
              if [ -n "$SUBMISSION_ID" ]; then
                echo "📋 Fetching rejection log..."
                xcrun notarytool log "$SUBMISSION_ID" \
                  --apple-id "$APPLE_ID" \
                  --password "$APPLE_APP_PASSWORD" \
                  --team-id "$APPLE_TEAM_ID" || true
              fi
              return 1
            fi
            
            echo "✅ $NAME notarized successfully!"
            return 0
          }

          # Notarize DMG
          if ! notarize_file "$DMG_FILE" "DMG"; then
            exit 1
          fi

          # Notarize PKG  
          if ! notarize_file "$PKG_FILE" "PKG"; then
            exit 1
          fi

          echo "📎 Stapling notarization ticket to DMG..."
          xcrun stapler staple "$DMG_FILE"

          echo "📎 Stapling notarization ticket to PKG..."
          xcrun stapler staple "$PKG_FILE"

          echo "✅ Notarization complete!"

      - name: Extract commit message
        id: commit
        env:
          COMMIT_MSG: ${{ github.event.head_commit.message }}
        run: |
          # Get the commit message (first line only, without the [release x.x.x] tag)
          MSG=$(echo "$COMMIT_MSG" | head -n1 | sed 's/\[release [0-9.]*\]//' | xargs)
          echo "message=$MSG" >> $GITHUB_OUTPUT
          # Get full commit body for changelog
          BODY=$(echo "$COMMIT_MSG" | tail -n +2)
          echo "body<<EOF" >> $GITHUB_OUTPUT
          echo "$BODY" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ needs.check-release.outputs.version }}
          name: Mira v${{ needs.check-release.outputs.version }}
          body: |
            ## What's New
            ${{ steps.commit.outputs.message }}
            ${{ steps.commit.outputs.body }}

            ---

            ### Installation
            **Preferred:** `Mira-Installer.pkg` (updates existing installs)
            1. Download `Mira-Installer.pkg`
            2. Run the installer
            3. Launch Mira from Applications

            **Alternative:** `Mira-${{ needs.check-release.outputs.version }}-macOS.dmg`
            1. Open the DMG and drag Mira to Applications
            2. Launch Mira from Applications

            **Requirements:** macOS 14.0 (Sonoma) or later

            ✅ **This release is signed and notarized by Apple** - no Gatekeeper warnings!
          files: |
            Mira-${{ needs.check-release.outputs.version }}-macOS.dmg
            Mira-Installer.pkg
          generate_release_notes: true
          draft: false
          prerelease: false

      - name: Sign DMG for Sparkle
        env:
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
        run: |
          VERSION="${{ needs.check-release.outputs.version }}"
          DMG_FILE="Mira-${VERSION}-macOS.dmg"

          # Find the sign_update tool
          SIGN_TOOL=""
          if [ -f ".build/artifacts/sparkle/Sparkle/bin/sign_update" ]; then
            SIGN_TOOL=".build/artifacts/sparkle/Sparkle/bin/sign_update"
          elif [ -f ".build/checkouts/Sparkle/sign_update" ]; then
            SIGN_TOOL=".build/checkouts/Sparkle/sign_update"
          fi

          if [ -z "$SIGN_TOOL" ]; then
            echo "❌ sign_update tool not found"
            exit 1
          fi

          # Sign the DMG using the EdDSA private key
          if [ -n "$SPARKLE_PRIVATE_KEY" ]; then
            echo "🔐 Signing DMG with EdDSA key..."
            SIGNATURE=$("$SIGN_TOOL" --ed-key-file <(echo "$SPARKLE_PRIVATE_KEY") "$DMG_FILE" | grep "sparkle:edSignature" | sed 's/.*sparkle:edSignature="\([^"]*\)".*/\1/')

            if [ -z "$SIGNATURE" ]; then
              # Alternative: sign_update might just output the signature
              SIGNATURE=$("$SIGN_TOOL" --ed-key-file <(echo "$SPARKLE_PRIVATE_KEY") "$DMG_FILE" 2>&1 | tr -d '\n')
            fi

            echo "SPARKLE_SIGNATURE=$SIGNATURE" >> $GITHUB_ENV
            echo "✅ DMG signed: ${SIGNATURE:0:20}..."
          else
            echo "⚠️ SPARKLE_PRIVATE_KEY not set - appcast will not have signature"
            echo "SPARKLE_SIGNATURE=" >> $GITHUB_ENV
          fi

      - name: Update Appcast
        env:
          COMMIT_MSG: ${{ github.event.head_commit.message }}
        run: |
          VERSION="${{ needs.check-release.outputs.version }}"
          DMG_URL="https://github.com/Snupai/Mira-SwiftUI/releases/download/v${VERSION}/Mira-${VERSION}-macOS.dmg"
          DMG_SIZE=$(stat -f%z "Mira-${VERSION}-macOS.dmg" 2>/dev/null || echo "10000000")
          PUB_DATE=$(date -R)
          SIGNATURE="${SPARKLE_SIGNATURE:-}"

          # Get commit message - title and body
          COMMIT_TITLE=$(echo "$COMMIT_MSG" | head -n1 | sed 's/\[release [0-9.]*\]//' | xargs)
          COMMIT_BODY=$(echo "$COMMIT_MSG" | tail -n +3)

          # Build release notes HTML
          RELEASE_NOTES="<style>body{font-family:-apple-system,BlinkMacSystemFont,sans-serif;font-size:13px;line-height:1.5;color:#333}h2{font-size:16px;margin:0 0 12px 0;color:#1a1a1a}h3{font-size:14px;margin:16px 0 8px 0;color:#444}ul{margin:8px 0;padding-left:20px}li{margin:4px 0}p{margin:8px 0}.version{color:#666;font-size:12px}</style>"
          RELEASE_NOTES="${RELEASE_NOTES}<h2>🎉 ${COMMIT_TITLE}</h2>"

          # Convert commit body to HTML list if it has bullet points
          if [ -n "$COMMIT_BODY" ]; then
            # Check if body has bullet points (lines starting with -)
            if echo "$COMMIT_BODY" | grep -q "^- "; then
              RELEASE_NOTES="${RELEASE_NOTES}<ul>"
              echo "$COMMIT_BODY" | grep "^- " | while read -r line; do
                ITEM=$(echo "$line" | sed 's/^- //')
                RELEASE_NOTES="${RELEASE_NOTES}<li>${ITEM}</li>"
              done
              RELEASE_NOTES="${RELEASE_NOTES}</ul>"
            else
              # Plain text body
              BODY_HTML=$(echo "$COMMIT_BODY" | sed 's/$/<br>/g')
              RELEASE_NOTES="${RELEASE_NOTES}<p>${BODY_HTML}</p>"
            fi
          fi

          RELEASE_NOTES="${RELEASE_NOTES}<p class=\"version\">Version ${VERSION} • <a href=\"https://github.com/Snupai/Mira-SwiftUI/releases/tag/v${VERSION}\">Full Changelog</a></p>"

          # Build enclosure attributes
          ENCLOSURE_ATTRS="url=\"${DMG_URL}\" sparkle:version=\"${VERSION}\" sparkle:shortVersionString=\"${VERSION}\""
          if [ -n "$SIGNATURE" ]; then
            ENCLOSURE_ATTRS="${ENCLOSURE_ATTRS} sparkle:edSignature=\"${SIGNATURE}\""
          fi
          ENCLOSURE_ATTRS="${ENCLOSURE_ATTRS} length=\"${DMG_SIZE}\" type=\"application/octet-stream\""

          # Create appcast.xml using printf to avoid heredoc YAML issues
          printf '%s\n' '<?xml version="1.0" encoding="utf-8"?>' > appcast.xml
          printf '%s\n' '<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">' >> appcast.xml
          printf '%s\n' '    <channel>' >> appcast.xml
          printf '%s\n' '        <title>Mira Updates</title>' >> appcast.xml
          printf '%s\n' '        <link>https://github.com/Snupai/Mira-SwiftUI</link>' >> appcast.xml
          printf '%s\n' '        <description>Updates for Mira Invoice App</description>' >> appcast.xml
          printf '%s\n' '        <language>en</language>' >> appcast.xml
          printf '%s\n' '        <item>' >> appcast.xml
          printf '%s\n' "            <title>Mira ${VERSION}</title>" >> appcast.xml
          printf '%s\n' "            <link>https://github.com/Snupai/Mira-SwiftUI/releases/tag/v${VERSION}</link>" >> appcast.xml
          printf '%s\n' "            <sparkle:version>${VERSION}</sparkle:version>" >> appcast.xml
          printf '%s\n' "            <sparkle:shortVersionString>${VERSION}</sparkle:shortVersionString>" >> appcast.xml
          printf '%s\n' "            <description><![CDATA[${RELEASE_NOTES}]]></description>" >> appcast.xml
          printf '%s\n' "            <pubDate>${PUB_DATE}</pubDate>" >> appcast.xml
          printf '%s\n' "            <enclosure ${ENCLOSURE_ATTRS}/>" >> appcast.xml
          printf '%s\n' '            <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>' >> appcast.xml
          printf '%s\n' '        </item>' >> appcast.xml
          printf '%s\n' '    </channel>' >> appcast.xml
          printf '%s\n' '</rss>' >> appcast.xml

          echo "✅ Generated appcast.xml for version ${VERSION}"
          if [ -n "$SIGNATURE" ]; then
            echo "✅ Appcast includes EdDSA signature"
          else
            echo "⚠️ Appcast does NOT include signature - auto-updates may not work"
          fi

      - name: Commit Appcast
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add appcast.xml
          git commit -m "chore: update appcast for v${{ needs.check-release.outputs.version }}" || echo "No changes to commit"
          git push || echo "Nothing to push"

      - name: Cleanup Keychain
        if: always()
        run: |
          if [ -n "$KEYCHAIN_PATH" ] && [ -f "$KEYCHAIN_PATH" ]; then
            security delete-keychain "$KEYCHAIN_PATH" || true
          fi
          rm -f $RUNNER_TEMP/certificate.p12 || true
